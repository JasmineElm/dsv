renv::snapshot()
knitr::opts_chunk$set(echo = TRUE)
renv::install("DBI")
library("DBI")
# if DBI is not installed, install it
if (!require("DBI"))
renv::install("DBI",
lock = TRUE)
library("DBI")
# connect to the database
con <- dbConnect(RSQLite::SQLite(), "fake_data.sqlite")
if (!require("RSQLite"))
renv::install("RSQLite",
lock = TRUE)
